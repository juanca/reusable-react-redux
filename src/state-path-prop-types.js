const PropTypes = require('prop-types');

/*
 * Utility
 * Define the PropTypes needed for the `statePath` property
 * when passed to React components.
 */
module.exports = PropTypes.arrayOf(PropTypes.string);
